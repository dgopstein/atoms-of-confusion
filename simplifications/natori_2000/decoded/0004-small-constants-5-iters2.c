#include <stdio.h>
double l;
int main(int _, char **o, char **O) {
  return printf("%c\n",
      (_-- + 1 && _ + 4 && main(_, -8, _), _ && o)
          ? (main(-1, ++o, O),
             ((l = (int)(o + 1) / (1 - (int)O * 2 - (int)O * (int)O),
               l * l > 3 && ((2 % 3) / 4 -
                                  2 + (l / 2)) < 1)["ab"]))
          : 'c');
}

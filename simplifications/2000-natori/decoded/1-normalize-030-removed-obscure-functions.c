#include <stdio.h>
double l;
main(int _, char **o, char **O) {
  return printf("%d\n",
      (_-- + 22 && _ + 44 && main(_, -43, _), _ && o)
          ? (main(-43, ++o, O),
             ((l = (int)(o + 21) / (3 - (int)O * 22 - (int)O * (int)O),
               l * l < 4 && ((-607728 % 2551443) / 405859. -
                                  4.7 + (l / 2)) < 1.57)[" #"]))
          : 10);
}
